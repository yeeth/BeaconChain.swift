import Foundation

let FAR_FUTURE_EPOCH = Epoch(UInt64.max)
let BASE_REWARDS_PER_EPOCH = 5
let DEPOSIT_CONTRACT_TREE_DEPTH = UInt64(2**5)
let SECONDS_PER_DAY = 86400
let JUSTIFICATION_BITS_LENGTH = 4
let ENDIANNESS = "little"

// Misc
let SHARD_COUNT = UInt64(2**10)
let TARGET_COMMITTEE_SIZE = UInt64(2**7)
let MAX_VALIDATORS_PER_COMMITTEE = UInt64(2**12)
let MIN_PER_EPOCH_CHURN_LIMIT = UInt64(2**2)
let CHURN_LIMIT_QUOTIENT = UInt64(2**16)
let SHUFFLE_ROUND_COUNT = 90
let MIN_GENESIS_ACTIVE_VALIDATOR_COUNT = UInt64(2**16)
let MIN_GENESIS_TIME = UInt64(1578009600)

// GWEI Values
let MIN_DEPOSIT_AMOUNT = UInt64(2**0 * UInt64(1e9))
let MAX_EFFECTIVE_BALANCE = UInt64(2**5 * UInt64(1e9))
let EJECTION_BALANCE = UInt64(2**4 * UInt64(1e9))
let EFFECTIVE_BALANCE_INCREMENT = UInt64(2**0 * UInt64(1e9))

// Initial Values
let GENESIS_SLOT = Slot(0)
let GENESIS_EPOCH = Epoch(0)
let BLS_WITHDRAWAL_PREFIX_BYTE = Data(repeating: 0, count: 1)

// Time parameters
let MIN_ATTESTATION_INCLUSION_DELAY = UInt64(2**0)
let SLOTS_PER_EPOCH = UInt64(2**6)
let MIN_SEED_LOOKAHEAD = UInt64(2**0)
let ACTIVATION_EXIT_DELAY = UInt64(2**2)
let SLOTS_PER_ETH1_VOTING_PERIOD = UInt64(2**10)
let SLOTS_PER_HISTORICAL_ROOT = UInt64(2**13)
let MIN_VALIDATOR_WITHDRAWABILITY_DELAY = UInt64(2**8)
let PERSISTENT_COMMITTEE_PERIOD = UInt64(2**11)
let MAX_EPOCHS_PER_CROSSLINK = UInt64(2**6)
let MIN_EPOCHS_TO_INACTIVITY_PENALTY = UInt64(2**2)

// State list lengths
let EPOCHS_PER_HISTORICAL_VECTOR = UInt64(2**16)
let EPOCHS_PER_SLASHINGS_VECTOR = UInt64(2**13)
let HISTORICAL_ROOTS_LIMIT = UInt64(2**24)
let VALIDATOR_REGISTRY_LIMIT = UInt64(2**40)

// Reward and penalty quotients
let BASE_REWARD_FACTOR = UInt64(2**6)
let WHISTLEBLOWER_REWARD_QUOTIENT = UInt64(2**9)
let PROPOSER_REWARD_QUOTIENT = UInt64(2**3)
let INACTIVITY_PENALTY_QUOTIENT = UInt64(2**25)
let MIN_SLASHING_PENALTY_QUOTIENT = UInt64(2**5)

// Max transactions per block
let MAX_PROPOSER_SLASHINGS = UInt64(2**4)
let MAX_ATTESTER_SLASHINGS = UInt64(2**0)
let MAX_ATTESTATIONS = UInt64(2**7)
let MAX_DEPOSITS = UInt64(2**4)
let MAX_VOLUNTARY_EXITS = UInt64(2**4)
let MAX_TRANSFERS = UInt64(0)
